name: CI/CD

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}/smartwatt-api

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: smartwatt-api/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r smartwatt-api/requirements.txt

      - name: Run test suite
        run: python -m pytest -q smartwatt-api/tests

  package:
    name: Package release artifact
    needs: test
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build tarball artifact
        run: |
          mkdir -p smartwatt-api/dist
          git archive --format=tar.gz --output "smartwatt-api/dist/smartwatt-api-${GITHUB_SHA}.tar.gz" HEAD:smartwatt-api

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: smartwatt-api-${{ github.sha }}
          path: smartwatt-api/dist/smartwatt-api-${{ github.sha }}.tar.gz
          retention-days: 14
